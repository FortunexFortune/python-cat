apiVersion: v1
kind: Service
metadata:
  name: python-cat
  namespace: python-cat
  labels:
    app: python-cat
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: python-cat
